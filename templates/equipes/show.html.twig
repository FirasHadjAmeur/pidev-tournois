{% extends 'base-back.html.twig' %}

{% block title %}Equipes{% endblock %}

{% block body %}
    <h1>Equipes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ equipe.nom }}</td>
            </tr>
            <tr>
                <th>NbrVictoire</th>
                <td>{{ equipe.nbrVictoire }}</td>
            </tr>
            <tr>
                <th>NbrPerdre</th>
                <td>{{ equipe.nbrPerdre }}</td>
            </tr>
            <tr>
                <th>NbrNulle</th>
                <td>{{ equipe.nbrNulle }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ equipe.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('equipes_index') }}">back to list</a>

    <a href="{{ path('equipes_edit', {'id': equipe.id}) }}">edit</a>

    {{ include('equipes/_delete_form.html.twig') }}
{% endblock %}
